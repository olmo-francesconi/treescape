[package]
name = "treescape-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Filesystem scan, tree, and squarified-treemap layout — the UI-agnostic core behind the `treescape` terminal disk-usage explorer."
readme = "../../README.md"
keywords = ["treemap", "disk-usage", "filesystem", "scan", "squarify"]
categories = ["algorithms", "filesystem", "visualization"]

[dependencies]
jwalk = { workspace = true }
humansize = { workspace = true }
